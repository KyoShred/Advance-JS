





/*const grid = document.querySelector('.grille');
const endScreen = document.querySelectorAll('.endScreen') || { innerHTML: '' };
let io = 0;

for (let i = 0; i < 240 ; i++){
    const squares = document.createElement('div');
    
    if (io == 0){
        squares.className = 'start';
    }

    if (io == 19){
        squares.className = 'end';
    }
    
    grid.appendChild(squares);

    io++;
    if (io == 20){
        io = 0;
    }
    console.log(i);

}

*/
 


/*document.addEventListener("keydown", function(event) {
    if (event.code === "Space") {
      console.log("Game Over");
      gameOver();
    }
  });


const gameOver = () => {
    endScreen.innerHTML = `<div class = 'gameOver'>
    </div>`;
    endScreen.style.visibility = 'visible';
    endScreen.style.opacité = '1';

}

let alien = [230];

let vaisseau = 230; 

alien.forEach(lol => console.log(lol));{
    if (alien > vaisseau){
        console.log('gameOver');
        document.write("gameOver");
    }
    else if (alien == vaisseau)
    {
        console.log('gameOver');
        document.write("gameOver");
        }
    
    else {}
}
*/


/*alert()

alert("ouikipetdua")*/


// Si la position de l'alien == la position du tireur alors fin de partie
// Si nombre alien est 0 == fin de partie 
// si case alien > 220 == fin de partie 
// Si la partie fini == afficher message game over 


/*
if (squares[currentShooterIndex].classlist.contains('invader','shooter')) {
    resultDisplay.innerHTML = 'game over'
    clearInterval(invadersId)

}

for (let i = 0; 1 < alienInvaders.length; i++) {
    if (alienInvaders[i] > squares.length){
        resultDisplay.innerHTML = 'game over'
        clearInterval(invadersId)
    }
}

invadersId = setInterval(moveInvaders, 200);




/*let playerDiv = document.getElementById("player");
let enemyDiv = document.getElementById("enemy");


let playerX = playerDiv
let playerY = playerDiv
let enemyX = enemyDiv
let enemyY = enemyDiv


function checkCollision() {


if (playerX == enemyX && playerY == enemyY){
    console.log("game over !");
    }
}

setInterval(checkCollision,200);


 
if (playerX === enemyX && playerY === enemyY) {}
  
if (playerX === enemyX && playerY === enemyY) {}
     
if (playerX === enemyX && playerY === enemyY) {}
    
if (playerX === enemyX && playerY === enemyY) {}*/
 









